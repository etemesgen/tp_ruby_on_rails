<!DOCTYPE html>
<html>
  <head>
    <title>TpRubyOnRails</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">
          <img src="https://i.ibb.co/tDjgKft/TASK-LIST.png" alt="TASK-LIST" width="52" height="52">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0 gap-5">
            <li class="nav-item btn btn-outline-warning border-0">
              <a class="nav-link active" aria-current="page" href="/">Accueil</a>
            </li>
            <li class="nav-item btn btn-outline-warning border-0">
              <a class="nav-link" href="/faq">FAQ</a>
            </li>
            <li class="nav-item btn btn-outline-warning border-0">
              <a class="nav-link" href="/contact">Contact</a>
            </li>
            <% if logged_in? %>
              <li class="nav-item btn btn-outline-warning border-0">
                <%= link_to 'Profile', edit_user_path(current_user), class: 'nav-link' %>
              </li>
            <% end %>
          </ul>
          <% if logged_in? %>
            <li class="nav-item btn btn-danger">
              <a class="nav-link" href="#" onclick="event.preventDefault(); document.getElementById('logout-form').submit();">Deconnexion</a>
              <%= form_with(url: deconnexion_path, method: :delete, id: 'logout-form', style: 'display: none;') do %><% end %>
            </li>
          <% else %>
            <li class="nav-item btn btn-warning">
              <a class="nav-link" href="/connexion">Connexion</a>
            </li>
          <% end %>
        </div>
      </div>
    </nav>

    <div class="container mt-4">
      <% flash.each do |key, message| %>
        <div class="alert alert-<%= key %>"><%= message %></div>
      <% end %>
    </div>

    <%= yield %>
  </body>
</html>
